/**
 * Returns the sum of the 2 numbers supplied
 *
 * @export
 * @param {number} a
 * @param {number} b
 * @returns {number}
 */
export function add(a: number, b: number): number
{
    return a + b;
}